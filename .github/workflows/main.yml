name: CI - Build and Push Flight Service

on:
  push:
    branches:
      - main

env:
  SERVICE: flight-service
  IMAGE_SHA: ${{ secrets.DOCKER_USERNAME }}/flight-service:${{ github.sha }}
  IMAGE_LATEST: ${{ secrets.DOCKER_USERNAME }}/flight-service:latest
  GITOPS_REPO: Travel-web-project/travel-gitops
  GITOPS_DEPLOYMENT_PATH: base/flight-service/kustomization.yaml
  GITOPS_BRANCH_PREFIX: ci/update-flight-service

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_SHA }}
            ${{ env.IMAGE_LATEST }}

      - name: Checkout GitOps repo
        uses: actions/checkout@v4
        with:
          repository: ${{ env.GITOPS_REPO }}
          path: gitops
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Kustomization
        run: |
          cd gitops/${{ env.GITOPS_DEPLOYMENT_PATH }}
          kustomize edit set image ${{ secrets.DOCKER_USERNAME }}/${{ env.SERVICE }}=${{ env.IMAGE_SHA }}

      - name: Commit and Push changes
        run: |
          cd gitops
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git checkout -b ${{ env.GITOPS_BRANCH_PREFIX }}-${{ github.sha }}
          git add .
          git commit -m "Update ${{ env.SERVICE }} image to ${{ env.IMAGE_SHA }}"
          git push origin HEAD
